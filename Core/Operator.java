public class Operator {
    public static void main(String[] args) {
        boolean x = true;
        boolean y = true;
        System.out.println(x && y);

        // && and || and ! : these are logical operators
        // &, | and ~ these are bitwise operators
    }
}
